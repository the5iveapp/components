import tokens from "@the5ive/tokens";
import { React } from "react";
export const Button = () => {
  return (
    <button style={{ backgroundColor: tokens.primary }}>
      Button
    </button>
  )
}
